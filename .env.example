# OpenAI (script generation)
OPENAI_API_KEY=

# ElevenLabs (TTS)
ELEVENLABS_API_KEY=

# Google Cloud TTS (alternative)
GOOGLE_APPLICATION_CREDENTIALS=

# Logging
LOG_LEVEL=INFO

# Pipeline defaults
DEFAULT_DURATION_SEC=30

# Docker user mapping (run: id -u / id -g to get your values)
UID=1000
GID=1000
